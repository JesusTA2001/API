# Archivo de configuración para Azure App Service
# Especifica el comando de inicio para la aplicación

cd /home/site/wwwroot && gunicorn -w 4 -k uvicorn.workers.UvicornWorker src.main:app --bind 0.0.0.0:8000
